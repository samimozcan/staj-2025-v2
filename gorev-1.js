/**
 * this function returns the sum of two numbers.
 *
 * @param {*} a
 * @param {*} b
 * @returns
 */

function mul(a, b) {
  return a * b;
}
function multiply(a, b) {
  return a * b;
}
function sum2(a, b) {
  return d + c;
}
function diff(a, b) {
  console.log("diff function called");
  console.log("a:", a, "b:", b);
  return a + b;
}

diff(5, 6);
